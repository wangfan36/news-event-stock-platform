# Paths may be absolute or relative to the repository root.
artifacts_dir: artifacts

# Optional local market data. Leave blank to use the built-in research demo and
# network providers only. Personal paths should go in config/local.yaml, which
# is excluded from Git.
market_data:
  parquet_path: ""
  universe_path: ""
  price_override_path: artifacts/data/price_overrides.json
